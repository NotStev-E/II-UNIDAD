
// Se utiliza la palabra import y entre llaves
// el nombre del objeto seguido de from y entre
// comillas la direccion del archivo que lo contiene
import { sumar } from "./operadores.js";
import { restar } from "./operadores.js";

// FUNCIÓN QUE SUMA DOS NÚMEROS

sumar(10, 5)
restar(78, 8);