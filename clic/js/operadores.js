
// La palabra export indica que otros archivos 
// tienen acceso al código
export function sumar(a, b){
    var resultado = a + b;
    console.log(resultado);
}

export function restar (a, b){
    var resultado = a - b;
    console.log(resultado);
}